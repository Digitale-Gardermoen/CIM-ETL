const mongoose = require('mongoose');

// connect and create admin credentials.

// change config file on the install location to enable auth.

// create DB and user, ask for PW input. We need to update this in .env

// create collection and index(es)

// disable default account and make a way to recover it.